[Unit]
Description=MicroSOCKS SOCKS5 Proxy
After=network.target wg-quick@wg0.service

[Service]
Type=simple
ExecStart=/usr/bin/microsocks -i 0.0.0.0 -p 1080 -u proxyuser -P CHANGE_ME_SOCKS_PASS
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
