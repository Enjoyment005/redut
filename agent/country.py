# -*- coding: utf-8 -*-
"""country.py — политика стран: чёрный список (никогда) + умная оценка остальных.

Решение владельца 2026-08-15. Раньше действовал жёсткий белый список: покупать
разрешалось только из 17 перечисленных стран, а весь СНГ был захардкожен в блок.
Теперь правило другое:

  * **чёрный список — навсегда**: Россия, Украина, Беларусь. Оттуда никогда не
    покупаем и такой выход не используем (для обхода блокировок он бессмыслен,
    да и юридически это не то, что нужно). Список можно только РАСШИРИТЬ через
    `config['countries']['blacklist']`, сузить — нельзя (предохранитель в коде);
  * **все прочие страны разрешены**, но получают оценку. Она складывается из
    репутации страны у сервисов и из того, **сходятся ли geoip-базы** в её
    определении. Оценка идёт добавкой к скорингу пробы (§7.4) — значит при прочих
    равных автоматика сама выберет Латвию, а не Нигерию, но человек волен
    применить любой прокси вне чёрного списка.

Почему репутация вообще важна (случай 2026-08-15): прокси `203.0.113.77` продан
как нигерийский, ip-api видит Нигерию, ipinfo — Нью-Йорк. Для сайта это «страна
скачет» — типовая реакция антифрода: капчи, отказ оплаты, требование верификации.
Технически прокси исправен, поэтому дисквалифицировать его нельзя — но и выбирать
его первым, когда рядом есть Рига, не нужно. Ровно это и делает оценка.

**2026-08-17: насколько сильно страна влияет на выбор — решает админ.** Появились
четыре стратегии (`config['countries']['strategy']`, переключаются в панели): от
«только избранные страны» до «страна не важна, решают замеры скорости». Чёрный список
вне выбора — его не снимает ни одна стратегия. Подробности — у STRATEGIES ниже.

Модуль намеренно без зависимостей: его импортируют probe (скоринг), money (гейт
покупки), agent/states (авто-подбор страны) и webpanel (объяснения в интерфейсе).
"""

# ── 1. Чёрный список: никогда, ни покупать, ни использовать ────────────────
# Копии этой константы держат probe.HARD_BLOCK_CC и providers.base.HARD_BLOCK_CC
# (независимые предохранители на разных слоях); tests/test_provider_money.py
# сверяет, что они не разошлись.
BLACKLIST_CC = frozenset({"ru", "ua", "by"})

# ── 2. Репутация страны у сервисов (банки, платёжки, магазины, антифрод) ───
# Оценка — не «хорошая/плохая страна», а вероятность лишних проверок на выходе.
TRUSTED_CC = frozenset({          # ЕС/ЕЭЗ + Швейцария, Британия, США, Канада
    "fi", "ee", "lv", "lt", "se", "no", "dk", "is", "de", "nl", "be", "lu",
    "at", "ch", "ie", "gb", "fr", "it", "es", "pt", "pl", "cz", "sk", "si",
    "hr", "hu", "ro", "bg", "gr", "mt", "cy", "us", "ca"})
GOOD_CC = frozenset({             # прочие развитые: репутация ровная, но дальше
    "jp", "kr", "sg", "hk", "tw", "au", "nz", "il", "ae", "qa", "uy", "cl"})
LOW_TRUST_CC = frozenset({        # чаще всего ловят капчи/отказы оплаты
    # соседи РФ — до 2026-08-15 были в жёстком блоке, теперь просто низкий рейтинг
    "kz", "kg", "tj", "uz", "tm", "am", "az", "md", "ge",
    # регионы, которые антифрод исторически считает высокорисковыми
    "ng", "gh", "ke", "cm", "ci", "sn", "tz", "ug", "zw", "za",
    "id", "vn", "ph", "bd", "pk", "in", "lk", "np", "mm", "kh", "la",
    "br", "mx", "ve", "co", "pe", "bo", "py", "ec", "gt", "hn", "ni", "do", "ht", "jm",
    "iq", "ir", "sy", "af", "ye", "ly", "sd", "so", "tr", "eg", "ma", "dz", "tn"})

RATING_TRUSTED = 25
RATING_GOOD = 12
RATING_NEUTRAL = 0
RATING_LOW = -25
GEO_MISMATCH_PENALTY = -20   # geoip-базы разошлись: для сайтов «страна скачет»
MIN_AUTO_RATING = 0          # ниже — автоматика сама не покупает (человек может)

# ── 3. Стратегия: КАК сильно страна влияет на выбор (настройка админа, 17.08) ──
#
# До 17.08 политика была одна и зашита в код — «репутация решает». Она хороша по
# умолчанию, но подходит не всем: кому-то нужен стабильно европейский выход (банк-
# клиент, рабочие сервисы), а кому-то важнее самый быстрый канал, и он готов мириться
# с капчами. Поэтому правило вынесено в `config['countries']['strategy']`.
#
# ЧЁРНЫЙ СПИСОК ВНЕ ВЫБОРА: ни одна стратегия не разрешает ru/ua/by — это по-прежнему
# предохранитель в коде, а не настройка (§6.1).
#
# Стратегия влияет ровно на три решения и больше ни на что:
#   1) где автоматике МОЖНО покупать       -> auto_allowed / money.buy_candidates
#   2) в каком порядке перебирать пул      -> states.rank_candidates
#   3) какой вес у страны в оценке пробы   -> probe.score (через rating)
#
# Поля стратегии:
#   weight         — множитель репутации (и штрафа за расхождение geoip) в оценке.
#                    0.0 = страна не влияет на выбор вообще, решают замеры;
#   country_first  — страна как ПЕРВИЧНЫЙ ключ сортировки кандидатов (иначе репутация
#                    просто складывается с результатом замеров, и быстрый прокси
#                    рискованной страны может обогнать медленный надёжной);
#   auto_gate      — автоматика сама покупает только страны с репутацией >= 0
#                    (рискованные остаются доступны человеку вручную);
#   only_whitelist — покупать разрешено лишь из `countries.whitelist`, а прокси прочих
#                    стран уходят в конец очереди выбора (но НЕ выбрасываются: живой
#                    выход из «неправильной» страны лучше аварии без выхода вовсе).
STRATEGIES = {
    "whitelist": {
        "title": "Только избранные страны",
        "short": "покупаем строго из своего списка стран",
        "desc": ("Автоматика покупает прокси только в странах из списка «избранные» "
                 "(по умолчанию 17 европейских + США и Канада, ближние первыми). Прокси "
                 "других стран панель не купит, а из уже имеющихся возьмёт только если "
                 "выбора нет вовсе — лучше чужая страна, чем сидеть без выхода. "
                 "Самый предсказуемый режим: годится, когда банк или рабочий сервис "
                 "привязан к региону и «прыжки» страны недопустимы."),
        "weight": 1.0, "country_first": True, "auto_gate": True, "only_whitelist": True,
    },
    "reputation": {
        "title": "Репутация страны",
        "short": "сначала надёжная страна, потом скорость",
        "desc": ("Страна решает первой: Европа и США вперёд, рискованные страны "
                 "автоматика сама не покупает (человек — может, вручную). Среди стран "
                 "одинаковой репутации выигрывает тот прокси, что лучше по замерам. "
                 "Так меньше капч, отказов оплаты и просьб «подтвердите личность». "
                 "Режим по умолчанию — с ним панель работала до появления выбора."),
        "weight": 1.0, "country_first": True, "auto_gate": True, "only_whitelist": False,
    },
    "balanced": {
        "title": "Баланс: репутация и замеры",
        "short": "репутация весит вполовину, замеры могут её перевесить",
        "desc": ("Репутация страны идёт не первым ключом, а слагаемым с половинным весом: "
                 "заметно более быстрый и стабильный прокси рискованной страны обгонит "
                 "медленный европейский. Покупать разрешено везде вне чёрного списка, "
                 "но при равных условиях панель всё равно возьмёт надёжную страну. "
                 "Разумный компромисс, если европейские прокси часто мрут или тормозят."),
        "weight": 0.5, "country_first": False, "auto_gate": False, "only_whitelist": False,
    },
    "speed": {
        "title": "Скорость и отклик",
        "short": "страна не важна, решают только замеры",
        "desc": ("Страна не влияет на выбор совсем (кроме чёрного списка): и порядок "
                 "перебора, и оценка считаются чисто по замерам — задержка, доступность "
                 "Telegram, работают ли оба порта, история отказов. Покупка идёт по "
                 "списку «избранных» стран как по списку ближайших. Самый быстрый канал, "
                 "но выход может оказаться в стране, которую банки и магазины встречают "
                 "капчей и отказом оплаты."),
        "weight": 0.0, "country_first": False, "auto_gate": False, "only_whitelist": False,
    },
}
DEFAULT_STRATEGY = "reputation"     # как было до появления выбора — обратная совместимость
OFF_WHITELIST_PENALTY = -60         # «не из избранных» при стратегии whitelist


def norm(cc):
    return (str(cc or "").strip().lower() or None)


def strategy(cfg=None):
    """id действующей стратегии. Неизвестное/пустое значение -> по умолчанию."""
    s = norm(((cfg or {}).get("countries") or {}).get("strategy"))
    return s if s in STRATEGIES else DEFAULT_STRATEGY


def strategy_info(cfg=None, name=None):
    """Описание стратегии (с полем id): для панели, журнала и объяснений."""
    st = name if name in STRATEGIES else strategy(cfg)
    return dict(STRATEGIES[st], id=st)


def whitelist(cfg=None):
    """Избранные страны из конфига (для стратегии whitelist — фильтр, для остальных —
    подсказка «начни отсюда», по возрастанию задержки из РФ). Чёрный список вычищается
    всегда, даже если кто-то впишет его сюда."""
    cc = ((cfg or {}).get("countries") or {}).get("whitelist")
    if not cc:
        # ленивый импорт: модуль остаётся независимым (см. шапку), а список стран
        # не размножается ещё одной копией
        from providers.base import DEFAULT_WHITELIST_CC
        cc = DEFAULT_WHITELIST_CC
    bl = blacklist(cfg)
    return [c for c in (norm(x) for x in cc) if c and c not in bl]


def blacklist(cfg=None):
    """Чёрный список: код + необязательное расширение из конфига. Только РАСШИРЯЕТ."""
    extra = ((cfg or {}).get("countries") or {}).get("blacklist") or []
    return set(BLACKLIST_CC) | {c for c in (norm(x) for x in extra) if c}


def is_blocked(cc, cfg=None):
    c = norm(cc)
    return bool(c) and c in blacklist(cfg)


def reputation(cc, geo_agree=True, cfg=None):
    """Репутация САМОЙ страны: None — запрещена, иначе число (больше = меньше проблем).

    От стратегии не зависит: рискованная страна остаётся рискованной, что бы админ
    ни выбрал — стратегия решает лишь, насколько сильно это влияет на выбор (rating).
    geo_agree=False — базы geoip разошлись в определении страны этого IP.
    Неизвестная страна (None) — нейтральна: незнание не повод ни блокировать,
    ни поощрять.
    """
    c = norm(cc)
    if c and c in blacklist(cfg):
        return None
    if c is None:
        base = RATING_NEUTRAL
    elif c in TRUSTED_CC:
        base = RATING_TRUSTED
    elif c in GOOD_CC:
        base = RATING_GOOD
    elif c in LOW_TRUST_CC:
        base = RATING_LOW
    else:
        base = RATING_NEUTRAL
    return base + (0 if geo_agree else GEO_MISMATCH_PENALTY)


def rating(cc, geo_agree=True, cfg=None):
    """Вклад страны в выбор при действующей стратегии: None — запрещена.

    Это репутация, помноженная на вес стратегии (у «скорости» вес 0 — страна не
    влияет ни на порядок, ни на оценку), плюс штраф «не из избранных» у стратегии
    whitelist. Именно это число складывает probe.score и сортирует rank/rank_candidates.
    """
    rep = reputation(cc, geo_agree, cfg)
    if rep is None:
        return None
    st = STRATEGIES[strategy(cfg)]
    r = rep * st["weight"]
    if st["only_whitelist"] and norm(cc) not in whitelist(cfg):
        r += OFF_WHITELIST_PENALTY
    return round(r, 1)


def auto_allowed(cc, geo_agree=True, cfg=None):
    """Может ли автоматика САМА купить прокси в этой стране (человек — шире, вручную)."""
    rep = reputation(cc, geo_agree, cfg)
    if rep is None:
        return False                                   # чёрный список — никогда
    st = STRATEGIES[strategy(cfg)]
    if st["only_whitelist"] and norm(cc) not in whitelist(cfg):
        return False                                   # «только избранные»
    if st["auto_gate"] and rep < MIN_AUTO_RATING:
        return False                                   # рискованную — только руками
    return True


def tier(cc, geo_agree=True, cfg=None):
    """Короткая метка для интерфейса и журнала (репутация страны, без стратегии)."""
    r = reputation(cc, geo_agree, cfg)
    if r is None:
        return "blocked"
    if not geo_agree:
        return "disputed"
    if r >= RATING_TRUSTED:
        return "trusted"
    if r >= RATING_GOOD:
        return "good"
    if r <= RATING_LOW:
        return "risky"
    return "neutral"


def explain(cc, geo_agree=True, cfg=None):
    """Человеческое объяснение — идёт в подсказку панели и в причину отказа."""
    c = norm(cc)
    t = tier(c, geo_agree, cfg)
    if t == "blocked":
        return "страна в чёрном списке — не покупаем и не используем никогда"
    if c and STRATEGIES[strategy(cfg)]["only_whitelist"] and c not in whitelist(cfg):
        return ("страна вне списка избранных, а выбрана стратегия «только избранные» — "
                "автоматика её не купит и возьмёт такой прокси лишь при полном отсутствии выбора")
    if t == "disputed":
        return ("geoip-базы расходятся в определении страны этого IP — для сайтов "
                "«страна скачет», это лишние капчи и проверки оплаты")
    if t == "trusted":
        return "надёжная страна выхода: Европа/США — минимум лишних проверок"
    if t == "good":
        return "нормальная страна выхода, но дальше от РФ — выше задержка"
    if t == "risky":
        return ("страна с высоким риском отказов: банки, платёжки и магазины "
                "часто требуют доп. подтверждения или блокируют оплату")
    return "нейтральная страна: явных проблем не известно"


def rank(candidates, cfg=None):
    """Отсортировать коды стран по убыванию оценки; запрещённые — выбросить.

    Порядок внутри одной оценки сохраняется входной (у вызывающего он осмысленный:
    например, «предпочитаемые» страны из конфига идут по возрастанию задержки).
    """
    out = []
    for i, cc in enumerate(candidates or []):
        c = norm(cc)
        r = rating(c, True, cfg)
        if r is None:
            continue
        out.append((-r, i, c))
    return [c for _, _, c in sorted(out)]
